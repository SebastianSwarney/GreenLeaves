using UnityEngine;

/// <summary>
/// Used in the 2d inventory grid, to detect if the player's mouse is this position.
/// Is used to determine where in the grid this icon is, and where the player is relative on the grid.
/// </summary>
public class Inventory_SlotDetector : MonoBehaviour
{
    public Vector2Int m_gridPos;
}
